/**
 * PM2 Configuration for Production Deployment
 * 
 * This configuration ensures the application runs reliably with:
 * - Automatic restarts on failure
 * - Environment variable management
 * - Log rotation
 * - Performance monitoring
 */

export default {
  apps: [{
    name: 'practice-intelligence',
    script: 'npm',
    args: 'run start',
    instances: 1,
    exec_mode: 'fork',
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    
    // Environment variables
    env: {
      NODE_ENV: 'production',
      PORT: process.env.PORT || 5000,
      
      // These will be pulled from Replit secrets or .env
      DATABASE_URL: process.env.DATABASE_URL,
      OPENAI_API_KEY: process.env.OPENAI_API_KEY,
      ANTHROPIC_API_KEY: process.env.ANTHROPIC_API_KEY,
      GOOGLE_CLIENT_ID: process.env.GOOGLE_CLIENT_ID,
      GOOGLE_CLIENT_SECRET: process.env.GOOGLE_CLIENT_SECRET,
      SESSION_SECRET: process.env.SESSION_SECRET || 'development-secret-change-in-production',
      
      // Feature flags
      AI_ENABLED: 'true',
      ENHANCED_TIMELINE: 'true',
      AUTO_CLEANUP: 'true',
      PERFORMANCE_MONITORING: 'true'
    },
    
    // Development environment (for testing)
    env_development: {
      NODE_ENV: 'development',
      PORT: 5000,
      DEBUG: 'app:*'
    },
    
    // Error handling
    error_file: 'logs/err.log',
    out_file: 'logs/out.log',
    log_file: 'logs/combined.log',
    time: true,
    
    // Graceful shutdown
    kill_timeout: 5000,
    listen_timeout: 3000,
    
    // Auto-restart configuration
    min_uptime: '10s',
    max_restarts: 10,
    
    // Performance monitoring
    instance_var: 'INSTANCE_ID',
    
    // Node.js flags
    node_args: '--max-old-space-size=1024',
    
    // Restart cron (optional - restart daily at 3 AM)
    // cron_restart: '0 3 * * *',
  }],
  
  // Deploy configuration (optional)
  deploy: {
    production: {
      user: 'node',
      host: 'your-server.com',
      ref: 'origin/main',
      repo: 'https://github.com/jonathanprocter/practice-intelligence_clients.git',
      path: '/var/www/practice-intelligence',
      'post-deploy': 'npm install && npm run build && pm2 reload ecosystem.config.js --env production',
      'pre-deploy-local': 'git push origin main'
    }
  }
};